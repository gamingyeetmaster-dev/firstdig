#cloud-config
# Paste this into the "custom data" / "user data" box when creating an Ubuntu VM.
# Replace the two values below first.
package_update: true
runcmd:
  - [ bash, -c, "curl -fsSL https://raw.githubusercontent.com/gamingyeetmaster-dev/firstdig/main/deploy/install.sh | REPO_URL=https://github.com/gamingyeetmaster-dev/firstdig.git DOMAIN=firstdig.app bash > /var/log/signals-install.log 2>&1" ]
